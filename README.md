AI Code Review

This AI-generated project contains a small server-driven UI SDK and a demo Todo app - to add and edit the Todo items in a list.

How to Run
- Backend: cd backend && npm install && npm start
- Frontend: cd frontend && npm install && npm run dev
